pragma solidity >= 0.7.0 < 0.9.0;


contract functionSample{
    
        function multipication(uint a , uint b) public view returns(uint){
        uint resulte = a*b;
        return resulte;
        
    }
}